@page
@model IndexModel
<main class="main-container">
    <div class="clamp">
        <div class="clamp-inner">
        </div>
    </div>
    <div class="sheet">
        <div class="left-circle"></div>
        <div class="right-circle"></div>
        <p class="top-name">MENU</p>
        <hr class="underline">
        <p class="underline-name ">Food and more</p>
        <table class="borderless-table">
            <tr>
                <th>Название</th>
                <th>Калорийность</th>
                <th>Продукты</th>
                <th>Вес одной порции</th>
                <th></th>
            </tr>
@*            @foreach (var recipe in RecipesService.GetRecipes())
            {
                <tr>
                    <td>@recipe.RecipeName</td>
                    <td>@recipe.CalorieValue</td>
                    <td>@recipe.IngredientsNeeded</td>
                    <td>@recipe.DishWeight</td>
                </tr>
            }*@
        </table>
    </div>
    <div class="button-container">
        <button><a asp-area="" asp-page="/addrecpt">Добавить</a></button>
        <button><a asp-area="" asp-page="/dellrecpt">Удалить</a></button>
        <button><a asp-area="" asp-page="/Editor">Редактор</a></button>
        <button>Подсчёт</button>
        <button><a asp-area="" asp-page="/Warehouse">Склад</a></button>
    </div>
    <div class="main-border"></div>
</main>
